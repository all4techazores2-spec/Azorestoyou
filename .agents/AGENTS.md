# Custom Workspace Rules

- **DO NOT Publish / Deploy Automatically**: Never run `npm run deploy`, `git push`, or any commands that publish or deploy the code to production automatically. Only make the code changes requested, build/verify them locally, and then explicitly ask the user for permission to publish/deploy.
